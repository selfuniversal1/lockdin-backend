services:
  - type: web
    name: lockdin-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    branch: main
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
